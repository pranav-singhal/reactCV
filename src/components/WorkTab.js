import React from 'react';

const WorkTab = (props) => {
    return(
        <div className={'work-tab'}>
            <h1>Split Me Up</h1>
            <p>
                It is an attempt to create a trustless method to backup private keys using Shamir’s Secret Sharing Algorithm.
                It started as a hack for EthIndia and we are finally able to launch a private alpha for our app
            </p>
        </div>
    )
}

export default WorkTab